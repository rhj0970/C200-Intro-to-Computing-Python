def factorial(n):
    """
    Using recusion, return the factorial of
    the number n
    Returns:An integer
    """
    if n == 1:
        return 1
    else:
        res = n*factorial(n-1)
    return res

def my_range(n, m):
    if n == m:
        return 0
    else:
        return [n]
    

    return -1

def palindrome(word):
    """
    Returns a boolean (True or False) if the string is
    a palindrome. Must do this recursively.
    Returns: Boolean
    """
    count = -1
    reversevalue = "" 
    for i in x:
        reversevalue = reversevalue+x[count]   
        count=count-1 
    Final = x == reversevalue 
    return Final
    return "a"

def removePos(x, lst):
    """
    Removes an item from the list 
    that is the same as x. Must do this recursively.
    Returns: List with an occurences of x gone
    """
    return -1

def sum2DList(lst):
    """
    Recursively go through and sum all the values from each row and 
    add the result of each row together to get a final value.
    Ex)
        [[1, 2, 3],
         [4, 5, 6],
         [7, 8, 9]] ---> 45
    Returns: Integer
    """
    return []

def print_chars_recursive(inpt, pos):
    """
    Print all characters from the list recursively.
    Returns: Nothing
    """
    pass

def recursive_factorial(n, isFirstRun):
    """
    Recursively computes the factorial and only writes to the file once
    Returns: Integer
    """
    return "a"

def recursive_file_sum(filename, fh, sm, isFirstRun):
    """
    Takes in a file and adds the numbers in the file. 
    Returns: Integer
    """
    return "A"

